1. O padrão de modelo nos permite projetar uma classe abstrata onde certos métodos são deixados para as subclasses implementarem.

2. Esse padrão permite gerar vários tipos de implementações, mantendo ao mesmo tempo uma interface comum

3. Ótimo para reutilização de código

4. Implementa uma estrutura de controle invertida onde a classe abstrata chama métodos nas subclasses
